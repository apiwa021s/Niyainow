# Public URLs (safe to expose to the browser). Production values are required
# at build time and must use HTTPS.
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ASSET_URL=https://images.example.com
# Optional Search Console ownership token (the content value only).
GOOGLE_SITE_VERIFICATION=

# Writer self-service (Studio, creator applications, and /api/studio/*).
# Keep disabled while the in-house translation workflow is being built.
WRITER_MODE_ENABLED=false

# PostgreSQL. Use a pooled managed connection string in serverless production.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/niyainow
DATABASE_MAX_CONNECTIONS=5

# Redis is an optional cache only; PostgreSQL remains the source of truth.
# Supplying REDIS_URL enables Redis. Use CACHE_ENABLED=false as a kill switch.
CACHE_ENABLED=true
REDIS_URL=
REDIS_TIMEOUT_MS=75
REDIS_DEBUG=false
REDIS_CACHE_PREFIX=niyainow
# Serialized values larger than this are served from PostgreSQL but not cached.
REDIS_MAX_ITEM_BYTES=1048576

# Reader abuse prevention. Tune these from production metrics. The optional
# hash secret isolates security identifiers from AUTH_SECRET rotation.
SECURITY_HASH_SECRET=
SECURITY_LOG_ENABLED=true
CHAPTER_RATE_LIMIT_5M=30
CHAPTER_RATE_LIMIT_1H=120
CHAPTER_RATE_LIMIT_ANONYMOUS_5M=15
CHAPTER_RATE_LIMIT_ANONYMOUS_1H=60
SCRAPER_SEQUENTIAL_THRESHOLD=10

# MongoDB source for the translated-novel import job.
MONGODB_URL=mongodb+srv://user:password@cluster.example.com/my-novel

# Private importer API used by C:\Winpp\Nvl. Configure the same secret as
# PENHOME_IMPORT_TOKEN in the desktop importer (minimum 32 characters).
NOVEL_IMPORT_TOKEN=

# Explicit safety interlock for the mock-catalog development seed. Keep false
# outside a disposable development/staging database.
ALLOW_DEVELOPMENT_SEED=false

# Auth.js — Google OAuth only. Generate AUTH_SECRET with at least 32 characters.
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_TRUST_HOST=true

# Cloudflare R2 (server-only except the public asset URL above). Disable r2.dev;
# the dedicated cookie-less public CDN/Worker MUST deny and never cache /staging/*,
# preserve verified final Content-Type, and add X-Content-Type-Options: nosniff.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=https://images.example.com
R2_UPLOAD_URL_TTL_SECONDS=300
 
# AI Translation Studio. Keep the value server-side; the database stores only
# this environment variable's name (AI_TRANSLATION_API_KEY), never the secret.
AI_TRANSLATION_API_KEY=
# Optional OpenAI-compatible endpoint. Defaults to the official OpenAI API.
AI_TRANSLATION_BASE_URL=https://api.openai.com/v1
# Maximum time for one AI step. Profile analysis with a reasoning model can take
# longer than one minute. Allowed range is 30000-240000 milliseconds.
AI_TRANSLATION_REQUEST_TIMEOUT_MS=180000
# QA warnings remain visible but do not block approval. Scores below this value
# receive focused correction; allowed range is 0-100.
AI_TRANSLATION_QA_MIN_SCORE=90
# Process different novels concurrently while preserving chapter order inside
# each workspace. Allowed range is 1-8.
TRANSLATION_WORKER_CONCURRENCY=2

# Stripe Checkout. The webhook endpoint is /api/webhooks/stripe. Before going
# live, Stripe must explicitly approve the NovelNow business/content category.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# debug | info | warn | error | silent
LOG_LEVEL=info
